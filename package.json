{
  "name": "discord-confessions-bot",
  "version": "1.0.0",
  "description": "Discord confession bot with admin attribution, aliases, reply threads, and role pings.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/store.js && node --check src/dashboard.js"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "dependencies": {
    "discord.js": "^14.21.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  }
}
